---
slogan: Installation
title: Get started with Tailwind CSS
description: |
  Tailwind CSS works by scanning all of your HTML files, JavaScript components, and any other templates for class names, generating the corresponding styles and then writing them to a static CSS file.

  It's fast, flexible, and reliable — with zero-runtime.
full: true
---

## Installation

<Tabs
  items={[
    "Using Vite",
    "Using PostCSS",
    "Tailwind CLI",
    "Framework Guides",
    "Play CDN",
  ]}
>
  <Tab value="Using Vite">
    <TabDescription>
      Installing Tailwind CSS as a Vite plugin is the most seamless way to
      integrate it with frameworks like Laravel, SvelteKit, React Router, Nuxt,
      and SolidJS.
    </TabDescription>

    <Steps>
      <Step>
        <StepTitle>Install Tailwind CSS</StepTitle>
        <StepDescription>Install `tailwindcss` and `@tailwindcss/vite` via npm.</StepDescription>
        <StepContent>
          ```sh title="Terminal"
          npm install tailwindcss @tailwindcss/vite
          ```
        </StepContent>
      </Step>

      <Step>
        <StepTitle>Configure the Vite plugin</StepTitle>
        <StepDescription>Add the `@tailwindcss/vite` plugin to your Vite configuration.</StepDescription>
        <StepContent>
          ```ts title="vite.config.ts"
          import { defineConfig } from 'vite'
          import tailwindcss from '@tailwindcss/vite'

          export default defineConfig({
            plugins: [
              tailwindcss(),
            ],
          })
          ```
        </StepContent>
      </Step>

      <Step>
        <StepTitle>Import Tailwind CSS</StepTitle>
        <StepDescription>Add an `@import` to your CSS file that imports Tailwind CSS.</StepDescription>
        <StepContent>
          ```css title="CSS"
          @import "tailwindcss";
          ```
        </StepContent>
      </Step>

      <Step>
        <StepTitle>Start your build process</StepTitle>
        <StepDescription>Run your build process with `npm run dev` or whatever command is configured in your `package.json` file.</StepDescription>
        <StepContent>
          ```sh title="Terminal"
          npm run dev
          ```
        </StepContent>
      </Step>

      <Step>
        <StepTitle>Start using Tailwind in your HTML</StepTitle>
        <StepDescription>Make sure your compiled CSS is included in the `<head>` (your framework might handle this for you), then start using Tailwind’s utility classes to style your content.</StepDescription>
        <StepContent>
          ```html title="HTML"
          <!doctype html>
          <html>
          <head>
            <meta charset="UTF-8">
            <meta name="viewport" content="width=device-width, initial-scale=1.0">
            <link href="/src/styles.css" rel="stylesheet">
          </head>
          <body>
            <h1 class="text-3xl font-bold underline">
              Hello world!
            </h1>
          </body>
          </html>
          ```
        </StepContent>
      </Step>
    </Steps>

  </Tab>

<Tab value="Using PostCSS">
  <TabDescription>
    Installing Tailwind CSS as a PostCSS plugin is the most seamless way to
    integrate it with frameworks like Next.js and Angular.
  </TabDescription>
</Tab>

<Tab value="Tailwind CLI">
  <TabDescription>
    The simplest and fastest way to get up and running with Tailwind CSS from
    scratch is with the Tailwind CLI tool. The CLI is also available as a
    [standalone executable](https://tailwindcss.com/blog/standalone-cli) if you
    want to use it without installing Node.js.
  </TabDescription>
</Tab>

<Tab value="Framework Guides">
  <TabDescription>
    Framework-specific guides that cover our recommended approach to installing
    Tailwind CSS in a number of popular environments.
  </TabDescription>
</Tab>

  <Tab value="Play CDN">
    <TabDescription>
      Use the Play CDN to try Tailwind right in the browser without any build step. The Play CDN is designed for development purposes only, and is not intended for production.
    </TabDescription>
  </Tab>
</Tabs>
